# StudentHub - Платформа выбора университета

современное веб-приложение для поиска и сравнения университетов Казахстана. Создано с использованием React, Django REST и PostgreSQL

## Возможности

- **Главная страница**: Красивая страница с популярными университетами и обзором платформы
- **Каталог университетов**: Просмотр 30+ университетов с расширенными фильтрами
  - Фильтрация по городу, стоимости обучения, форме обучения, программам и наличию общежития
  - Режимы отображения: сетка и список
- **Детальная информация**: Полные профили университетов с:
  - 3D виртуальными турами по кампусу через iframe
  - Фотогалереями
  - Списком программ обучения
  - Контактной информацией
- **Инструмент сравнения**: Сравнение до 3 университетов бок о бок
- **Избранное**: Сохранение и управление избранными университетами (хранится в localStorage)
- **Админ-панель**: Полная админ-панель Django для управления университетами и программами
- **Двуязычность**: Поддержка русского и казахского языков
- **AI-функции**: Интеллектуальный помощник и AI-анализ сравнения (см. раздел "AI Features" ниже)

## технологический стек

### Frontend (Фронтенд)
- **React 18** с Vite
- **React Router** для навигации
- **TailwindCSS** для стилизации
- **Lucide React** для иконок
- **Axios** для API запросов

### Backend (Бэкенд)
- **Django 5** с Django REST Framework
- **PostgreSQL 15** база данных
- **Django CORS Headers** для кросс-доменных запросов
- **Pillow** для обработки изображений
- **Gunicorn** для продакшн сервера
- **OpenAI API** для AI-функционала (GPT-4o-mini / GPT-4)

### DevOps
- **Docker** и **Docker Compose** для контейнеризации

## Структура проекта

```
datahub-rk-hackathon/
├── backend/
│   ├── config/                 # Конфигурация Django
│   │   ├── settings.py
│   │   ├── urls.py
│   │   └── wsgi.py
│   ├── universities/           # Основное приложение
│   │   ├── models.py          # Модели University, Program
│   │   ├── serializers.py     # DRF сериализаторы
│   │   ├── views.py           # API представления
│   │   ├── urls.py            # Маршрутизация URL
│   │   ├── admin.py           # Конфигурация админки
│   │   └── management/        # Команды управления (seed_data)
│   ├── ai/                     # AI-функционал
│   │   ├── views.py           # API представления для AI
│   │   ├── serializers.py     # Сериализаторы запросов/ответов
│   │   ├── urls.py            # Маршрутизация AI эндпоинтов
│   │   └── services/          # LLM сервисы
│   │       └── llm.py         # Интеграция с OpenAI
│   ├── Dockerfile
│   ├── entrypoint.sh
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── components/        # Переиспользуемые компоненты
│   │   │   ├── Navbar.jsx
│   │   │   ├── Footer.jsx
│   │   │   ├── UniversityCard.jsx
│   │   │   ├── FiltersPanel.jsx
│   │   │   ├── ComparisonTable.jsx
│   │   │   ├── FavoritesList.jsx
│   │   │   ├── LanguageSwitcher.jsx
│   │   │   └── ai/            # AI компоненты
│   │   │       ├── AIChatWidget.jsx
│   │   │       ├── AIChatMessage.jsx
│   │   │       └── CompareSummary.jsx
│   │   ├── pages/             # Страницы
│   │   │   ├── Home.jsx
│   │   │   ├── Universities.jsx
│   │   │   ├── UniversityDetails.jsx
│   │   │   ├── Compare.jsx
│   │   │   └── Favorites.jsx
│   │   ├── contexts/          # React контексты
│   │   │   └── LanguageContext.jsx
│   │   ├── services/          # API сервисы
│   │   │   ├── api.js
│   │   │   └── ai.js          # AI API сервис
│   │   ├── App.jsx
│   │   ├── main.jsx
│   │   └── index.css
│   ├── public/
│   │   └── images/            # Статические изображения
│   ├── Dockerfile
│   └── package.json
├── docker-compose.yml          # Конфигурация для разработки
└── README.md
```

## Быстрый старт

### Требования

- Docker и Docker Compose установлены
- Git

### Запуск с Docker

1. **Клонируйте репозиторий**
   ```bash
   git clone https://github.com/blxitze/datahub-rk-hackathon.git
   cd datahub-rk-hackathon
   ```

2. **Запустите приложение**
   ```bash
   docker-compose up --build
   ```

3. **Откройте приложение**
   - Фронтенд: http://localhost:5173
   - API бэкенда: http://localhost:8000/api/
   - Админ-панель: http://localhost:8000/admin/
     - Логин: `admin`
     - Пароль: `admin123`

### Ручная установка (для разработки)

#### Бэкенд

1. **Создайте виртуальное окружение**
   ```bash
   cd backend
   python -m venv venv
   source venv/bin/activate  # Windows: venv\Scripts\activate
   ```

2. **Установите зависимости**
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте PostgreSQL**
   
   Измените `config/settings.py` для SQLite:
   ```python
   DATABASES = {
       'default': {
           'ENGINE': 'django.db.backends.sqlite3',
           'NAME': BASE_DIR / 'db.sqlite3',
       }
   }
   ```

4. **Выполните миграции и загрузите данные**
   ```bash
   python manage.py migrate
   python manage.py seed_data
   python manage.py createsuperuser
   ```

5. **Запустите сервер**
   ```bash
   python manage.py runserver
   ```

#### Фронтенд

1. **Установите зависимости**
   ```bash
   cd frontend
   npm install
   ```

2. **Настройте URL API**
   
   Создайте файл `.env`:
   ```
   VITE_API_URL=/api
   ```

3. **Запустите сервер разработки**
   ```bash
   npm run dev
   ```

## API Эндпоинты

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/api/universities/` | Список всех университетов |
| GET | `/api/universities/{id}/` | Детали университета |
| GET | `/api/programs/` | Список всех программ |
| GET | `/api/filter-options/` | Доступные опции фильтрации |
| POST | `/api/ai/chat/` | AI чатбот для вопросов об университетах |
| POST | `/api/ai/compare-summary/` | AI сводка для сравнения университетов |

### Пример ответа

```json
{
  "id": 1,
  "name": "Назарбаев Университет",
  "city": "Астана",
  "description": "Автономный исследовательский университет мирового уровня...",
  "tuition": "0.00",
  "rating": "4.95",
  "programs": [...],
  "iframe_3d_tour_url": "https://...",
  "has_dormitory": true,
  "study_form": "full-time"
}
```

## Маршруты фронтенда

| Маршрут | Компонент | Описание |
|---------|-----------|----------|
| `/` | Home | Главная страница |
| `/universities` | Universities | Каталог университетов с фильтрами |
| `/universities/:id` | UniversityDetails | Детали университета |
| `/compare` | Compare | Инструмент сравнения |
| `/favorites` | Favorites | Список избранных |

## Конфигурация

### Переменные окружения

#### Бэкенд
| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `DEBUG` | Режим отладки | `True` |
| `DJANGO_SECRET_KEY` | Секретный ключ | Генерируется |
| `POSTGRES_DB` | Имя базы данных | `unihub` |
| `POSTGRES_USER` | Пользователь БД | `postgres` |
| `POSTGRES_PASSWORD` | Пароль БД | `postgres` |
| `POSTGRES_HOST` | Хост БД | `localhost` |
| `ALLOWED_HOSTS` | Разрешённые хосты | `localhost,127.0.0.1` |
| `OPENAI_API_KEY` | API ключ OpenAI | Требуется для AI |
| `OPENAI_MODEL` | Модель OpenAI | `gpt-4o-mini` |

#### Фронтенд
| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `VITE_API_URL` | URL API бэкенда | `/api` |

## Docker команды

```bash
# Разработка
docker-compose up --build           # Собрать и запустить
docker-compose up -d                # Запустить в фоне
docker-compose down                 # Остановить все сервисы
docker-compose logs -f backend      # Просмотр логов бэкенда
docker-compose restart backend      # Перезапуск бэкенда

# Выполнение команд Django
docker exec -it unihub-backend python manage.py createsuperuser
docker exec -it unihub-backend python manage.py seed_data
```

## Админ-панель

Доступ к админ-панели Django: `http://localhost:8000/admin/`

Возможности:
- Управление университетами (CRUD операции)
- Управление программами обучения
- Загрузка логотипов и изображений галереи
- Просмотр статистики

## Локализация

Приложение поддерживает два языка:
- **Русский** 
- **Казахский**

Переключение языка доступно в шапке сайта.

---

## AI Features (Функции искусственного интеллекта)

DataHub включает мощные AI-функции, работающие на базе OpenAI GPT.

### 🤖 AI Чатбот

Плавающий виджет чат-бота в правом нижнем углу страницы позволяет пользователям задавать вопросы об университетах.

**Возможности:**
- Ответы на вопросы об университетах Казахстана
- Информация о программах обучения, стоимости, общежитиях
- Помощь с выбором университета
- Поддержка русского и казахского языков

**Использование:**
1. Нажмите на круглую кнопку чата в правом нижнем углу
2. Введите вопрос об университетах
3. Получите мгновенный ответ от AI

### 📊 AI Сводка сравнения

На странице сравнения (`/compare`) доступна кнопка "AI Сводка", которая генерирует детальный анализ выбранных университетов.

**Возможности:**
- Анализ преимуществ и недостатков каждого университета
- Сравнение по ключевым критериям
- Персонализированные рекомендации
- Форматированный markdown-отчёт

**Использование:**
1. Добавьте минимум 2 университета для сравнения
2. Нажмите кнопку "AI Сводка"
3. Получите детальный анализ под таблицей сравнения

### ⚙️ Настройка AI

Для работы AI-функций необходимо установить API ключ OpenAI:

```bash
# В .env файле или переменных окружения
OPENAI_API_KEY=sk-your-api-key-here
OPENAI_MODEL=gpt-4o-mini  # или gpt-4, gpt-3.5-turbo
```

**Примечание:** Все обращения к OpenAI API происходят через бэкенд. API ключ никогда не передаётся на фронтенд.

### API Эндпоинты для AI

#### POST `/api/ai/chat/`
Отправка сообщения в чатбот.

**Запрос:**
```json
{
  "message": "Какие университеты есть в Алматы?",
  "conversation_history": []
}
```

**Ответ:**
```json
{
  "response": "В Алматы находятся следующие крупные университеты...",
  "success": true
}
```

#### POST `/api/ai/compare-summary/`
Генерация сводки для сравнения университетов.

**Запрос:**
```json
{
  "university_ids": [1, 4, 7]
}
```

**Ответ:**
```json
{
  "summary": "## Анализ сравнения\n\n### Университет 1...",
  "success": true,
  "universities_compared": 3
}
```

---

## 👥 Команда

**Bed Action Inc.**

---

Сделано с <3 для IT FEST 2025 командой Bed Action Inc.
